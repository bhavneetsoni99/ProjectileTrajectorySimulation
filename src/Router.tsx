// import * as React from 'react';
// // import { connect } from 'react-redux';

// import HomePageView from './HomePageView';

// const routeSwitch = (route: string) => {
//   switch (route) {
//     case 'HomePage':
//       return <HomePageView />;
//     default:
//       return <HomePageView />;
//   }
// };

// interface Props {}
// interface State {}
// interface Query {
//   route: string;
//   setState: any;
// }

// // const Router: React.Component<Props, State> = (query: Query) => {
// //   return routeSwitch(query.route);
// // };

// interface RootState {}

// // const mapStateToProps = (root: RootState) => ({
// //   route: 'HomePage',
// //   setState: { homeText: 'home after change ' },
// // });

// // export default connect(mapStateToProps)(Router);
